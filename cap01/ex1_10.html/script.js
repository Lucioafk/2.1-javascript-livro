const numero = window.Number(prompt("Informa um numero que iremos calcular o anterior e o posterior dele"))

numeromenos = numero - 1
numeromais = numero + 1

window.alert(`O numero anterior é ${numeromenos} e o posterior é ${numeromais}`)